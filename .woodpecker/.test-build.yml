pipeline:
  lint:
    image: node:14-alpine
    commands:
      - npm ci
      - npm run lint:js
  build:
    image: plugins/docker
    settings:
      repo: ${CI_REPO}
      dry_run: true
when:
  event:
    - pull_request
